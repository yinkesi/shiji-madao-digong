# 《实验史记·马刀地宫》百 bug 清册

> 2026-09-19 全量 QA：引擎级猴测（40 局 × 2500 动混合扰动）+ 定向系统探针（30 发实弹）+ Playwright 真人式 UI 走查 + 全量代码走读。
> 证据标注：**[实测]**=脚本复现；**[代码]**=代码走读定位；**[截图]**=testshots/ 可见；**[推演]**=数值推演。
> 定位格式 `文件:行`。本文档只记录、未修复；修复优先级建议见文末。

---

## A · 崩溃与错误

1. **[实测]** 标题页「修炼」购买任意节点即抛 `Uncaught TypeError: Cannot set properties of null (setting '_metaBonuses')`——`hud.js openCult` 购买回调调 `game.applyMetaBonuses()`，而标题屏 `game.G=null`。购买流程中断。
2. **[实测]** 承上：购买已扣文脉但面板文脉数不刷新（重渲染被异常打断），显示仍为购买前数值。
3. **[实测]** 关闭修炼面板后，标题屏「文脉/名册」统计仍是旧值（`showTitle` 只在进入时刷新一次）。

## B · 死内容：失效的卡/技/被动

4. **[实测]** 刀卡「刀扫一片」：`cleave` 全引擎无任何溅射实现（`js/engine.js` strike 无该分支）——整卡无效。
5. **[实测]** 刀卡「长杆马刀」：两格刀击未实现（`doMove` 只认相邻格 bump）——整卡无效。
6. **[实测]** 刀卡「卧薪尝胆」：玩家受击永不积怨（积怨分支只认 `hasP(def,'grudgeStack')` 即小川被动），增伤条件永假——整卡无效。
7. **[实测]** 刀卡「锦绣夜行」：`G._floorKnifeUsed` 只在 `roundStart` 场景外从未按层重置——全轮只有第一刀 +2，文案写「每层首次刀击 +2」。
8. **[实测]** 崇国被动「弃车保帅」（致命伤留 1 血回 3）永不触发：`kill()` 不查敌方 `lethalKeep`——八层镇守的核心机制是死代码，玩家实际在打一个无免死木桩。
9. **[实测]** 点将 wonder：被动「马刀之神」（血祭后三次翻倍）完全无效——他的技能列表里没有血祭（`newRunState` 只给音克思配血祭）。
10. **[实测]** 崇国「种树」技 AI 永不施展：`aiSkillTarget` 对 `kind:"summon"` 无分支返回 null——该技是死的（层规则的种树是另一套代码）。
11. **[实测]** 录得「破门而入」严重退化：`doSkill` 无 `blink` 分支，闪现消失、射程 99 缩成 1，变成一个平庸的近战封技。
12. **[实测]** 岳连奇（lianqi）有角色卡但不在任何层的 boss/elite 配置里——26 个有名角色中唯一全程遇不到的。
13. **[代码]** `js/data.js` shengxiang 的 passive 对象里混入 `skill: null` 脏字段。
14. **[代码]** `js/data.js` limo 顶层 `moveRange: 2` 与 `passive.moveRange: 2` 双写（`makeUnit` 只读 passive 那份，顶层是冗余源）。

## C · 死字段/死代码

15. **[代码]** `engine.js`：`G.stairsOpen` 每层写入但从不读取（开放与否全靠 `stairsOpenNow()` 现算）。
16. **[代码]** `engine.js`：`G.key`（锁门层的「钥匙」标记）设而不读——缴钥机制的残迹，实际判据是「为兵死没死」。
17. **[代码]** `engine.js`：`G.firstClears` 记录首通层但从未消费——「首通额外文脉赏」没有落地。
18. **[代码]** `main.js`：`game.animating`/`game.busyAnim` 恒为 false，`input.js busy()` 读的正是它——动画锁形同虚设。
19. **[代码]** `hud.js refreshHUD`：`G.player.bossTag ? "" : ""` 无意义表达式（残留代码）。
20. **[代码]** `ui.js`：`armed.set()` 时构造的 `targetInfo` 从未被渲染逻辑读取。
21. **[代码]** `run.js summarize()`：`rounds` 取 `G.round`——每层重置后无意义，且 UI 从未消费该字段。

## D · 玩法规则漏洞

22. **[实测]** 血祭在 1 血时自损 0 仍获 2 层翻倍（`pay = min(⌊hp/2⌋, hp-1)` 钳成 0）——濒血零成本拿最强增益。
23. **[实测]** 血祭蓄力（emp）无上限：满血反复血祭可囤任意层翻倍（猴测实测囤到 9；10% 随机乱放都能囤爆）。
24. **[实测]** 蓄力永不过期——无衰减、无回合上限，可带着一层层囤过去打 Boss 一波爆发。
25. **[实测]** 撞墙推进整个回合：`act()` 在 `doMove` 早退后照常 `enemiesAct`——乱按方向键等于给敌人白送行动。
26. **[代码]** 撞锁着的楼梯同理：`tryDescend` 只 log 一句「梯封未开」，回合照走。
27. **[代码]** 施技目标非法（点自己/超射程/无目标）只提示一句，回合照走——无效施法有代价且无确认。
28. **[代码]** 掷物（陀螺）无合法目标时「掷无可掷」，回合照走。
29. **[实测]** 玩家被晕零效果：`st.stun` 只有敌人回合会检查，玩家侧 `doMove/doSkill` 不看——子琛「体育课起义」的群体晕对玩家是空气。
30. **[实测]** 录技对所有点将生效（`kill()` 不判 runner 身份），与音克思被动「录技……仅史官可得」的文案直接矛盾——其他将白拿整套成长体系。

## E · 判定与数值

31. **[实测]** 「班主任的偏爱」每层实得 +6 盾：`enterFloor` 与 `tryDescend` 双双 +3，文案写 +3。
32. **[实测]** 看台飞瓶无视墙体：隔墙同行同列照样挨砸（`roundStart` 的 cans 判定只看行列坐标，无 LOS）——与「走位躲避」的预期玩法相悖。
33. **[实测]** 被飞瓶砸死时 `player.dead` 不置位（`playerDown()` 不设 dead；刀杀路径经 `kill()` 会设）——死后尸体继续渲染、`unitAt` 仍算活人占格。
34. **[实测]** 难度目力加成漏了杂兵：精英/镇守 `saw += d.saw`，杂兵恒 5——噩梦下精英千里眼、杂兵近视眼，行为不一致。
35. **[实测]** 简单档「敌伤 -1」对 1 伤杂兵无效（`max(1, 0)=1`）——第一层实际只有镇守降了伤，与难度描述体感不符。
36. **[实测]** 毒伤无视护盾：回合末 `u.hp -= 1` 直接扣血——「护盾先吃伤害」的直觉被穿透（至少应写进文案）。
37. **[实测]** 汶斌被动「秒之」文案限定「刀击三成双倍」，实现上技能/投掷物一律可会心——他的弹簧纸箭也能 ×2。
38. **[推演]** 大哥被动「城墙之梦」（贴墙刀击 +1）在前作战棋语境是城墙专属，地宫里满地是墙——精英/镇守版大哥几乎常驻 +1，数值强度较原意失控。
39. **[实测]** 敌人把楼梯当普通地板停上去（猴测 `enemy-on-stairs` ×144）：撞梯变撞人，下行被迫先清场；且敌人没有「离开楼梯」的倾向。
40. **[代码]** 起义援军落点用 `walkable()` 判定，可落在史料/木箱/灶间/商摊格上——把互动格压住。

## F · 经济与进度

41. **[实测]** 杂兵/精英被斩分文不掉（`kill()` 只给 Boss 发 15-25）——设计稿的 mob 2-4 / 精英 6-10 未实现，商摊经济只靠 Boss 与木箱。
42. **[实测]** 小卖部实际配置在 4/5/6/7/8 五层（`FLOORS.shop` 标记），所有文档（README/DESIGN/玩法说明/行囊面板）都写「2/4/6/8」。
43. **[实测]** 七层与九层都斩 wonder：`bossKills` 两次 push 同 id，结算页「斩镇守：王元昊、王元昊」。
44. **[代码]** 文脉结算无击杀分（设计稿 0.2/杀未实现）——清一场小怪战在结算里零存在感。
45. **[代码]** 无难度解锁链：玩法说明「收卷一次，噩梦的门就虚掩着」，实际五档开局全开。
46. **[文档]** 玩法说明「首次斩杀某层镇守，其人入册」未提崇国例外——斩八层镇守只掉刀卡不进名册，期望落差。

## G · UI/UX

47. **[实测]** 技能热键只有 1-9：注入 11 技实测仅 9 个有编号——第 10 技起键盘不可达。
48. **[实测]** 技能栏无上限、不换行：11 技+待机已 1096px，再多必溢出窄屏，右端技点不到。
49. **[实测]** 技能栏不显示血祭的代价（损当前半血）——栏上只有名字和射程，新手不看刀谱必误用。
50. **[实测]** 史官记事双前缀病句：「汝击汝以刀击万震，伤其2」——`engine.js:229` 的 `"汝击"` 前缀与 `hitLine` 内的「汝以刀击」叠加。
51. **[实测]** 精英/杂兵惊动无醒目提示：`alert` 事件只有 Boss 弹卡，精英被惊起只在小字日志里滚过——被偷袭了才知道。
52. **[实测]** 行囊投掷物病句 toast：「掷出之物流需点选目标——暂从行囊掷之」（`hud.js:94`）。
53. **[代码]** 灶间用后瓦片变回普通地板、无「已熄」痕迹；且「灶间已冷」分支永远不可达（瓦片都没了无从再撞）——死代码+视觉缺失双问题。
54. **[代码]** 镇守无常驻血条/名字条：Boss 卡只闪 2.4 秒，战况全靠格子下 3px 小血条。
55. **[代码]** 活着的敌人无法查看被动/技能：图鉴只收「已斩者」——战前情报为零，与「每层先读特则再决定带谁的刀法」的设计哲学脱节。
56. **[代码]** 自动行走点向不可达处（隔墙/未连通）静默无反馈：`pathTo` 返回 null 什么都不发生。
57. **[代码]** 键盘长按被 `e.repeat` 直接吞掉——无连走，长途移动全靠连打或鼠标，手感割裂。
58. **[代码]** 标题屏按 M 静默无效（`onKeyDown` 的 `!inGame` 早退在 M 之前），无任何提示。
59. **[代码]** 录技/掉刀卡/成就/购物 toast 全部同一样式，连弹时无法分辨类别。
60. **[代码]** 小地图不标商摊与木箱位（只画墙/地面/灶/梯/自己）——找商全靠腿。
61. **[代码]** 死亡到结算屏之间 900ms 空窗，无「你死了」的过渡反馈（玩家词穷地看尸体消失后的地板）。
62. **[代码]** 收卷与阵亡共用同一结算模板（仅标题换色）——通关没有差异化庆祝。
63. **[截图]** toast（z-index 60）压在结算/面板标题上方（`testshots/07-end.png` 可见重叠）。
64. **[代码]** 移动端媒体查询直接隐藏日志+小地图，且无虚拟方向键——窄屏体验是「砍功能」而非适配。
65. **[代码]** 结算页「再入宫」回调里残留 `location.hash = ""` 无用代码。
66. **[代码]** 「急击勿失」「击破回血」首次触发无教学 toast/高亮——核心机制只藏在「观卷」面板里，新档前几层全靠巧合发现。

## H · 文档与实现不符

67. **[实测]** 玩法说明「第九层斩终焉之 wonder……收卷」，实际斩完还须自行走到楼梯踩上去才判胜（`tryDescend` 在楼梯格判 win）。
68. **[代码]** 玩法说明 F6「缴为兵之钥以开梯」——实现为「为兵死即算」，并无钥匙道具。
69. **[代码]** DESIGN.md 商摊「2/4/6/8 层」与 `FLOORS` 实配 4-8 不符（同 42 的设计文档侧）。
70. **[代码]** DESIGN.md F8 精英「二中四人」实配三人（缺连奇——同 12）。
71. **[代码]** DESIGN.md 文脉「击杀 0.2/个」未实现（同 44 的设计文档侧）。
72. **[文档]** README 代码结构写「22 角色」，`DATA.CHARS` 实为 26 条。
73. **[文档]** README 测试章节未收录本次新增的 `tests/bughunt_*.mjs` 三脚本。
74. **[文档]** README 快速开始的 `for f in tests/test_*.mjs` 循环语法在 Windows cmd/PowerShell 不可用，未注明需 Git Bash。

## I · 平衡预警

75. **[推演]** 三层镇守鲁豪：被动刀击 ×2 作用于敌方 → 3×2=6 伤一刀，normal 18 血三刀倒，叠加「党争」小怪合围——第三层是全游戏难度悬崖（平衡哨兵只覆盖第一层）。
76. **[推演]** 噩梦 eHp×2 下八层崇国 60 血、九层 wonder 52 血+飞瓶+双精英——当前输出曲线（刀 2-5）是数十分钟消耗战。
77. **[代码]** 点将 DPS 差 3 倍无代价权衡：鲁豪起手刀 6 vs 万震刀 2——「换将即换整套」的选择退化成「无脑鲁豪」。
78. **[代码]** 修炼「拳不离手」满级后刀击 5：前四层杂兵 4 血全部一刀半解决——局外成长与敌方血量曲线错位，重玩前期失味。

## J · 工程卫生

79. **[代码]** 存档无版本迁移：`v` 字段存在但 `Meta.load` 只做浅合并，未来改结构必炸老档。
80. **[代码]** 每次行动同步全量 `JSON.stringify` 写 localStorage（44×26 地图+全部单位），低端机微卡顿源。
81. **[代码]** `input.js` 硬编码 `42`（瓦片尺寸）与 `ui.js TILE` 双源魔数——改瓦片尺寸必漏一处，点击命中错位。
82. **[代码]** `UID` 计数器在 deserialize 后从 1 重来，新造单位与存档单位 uid 冲突（当前事件按坐标不按 uid，属埋雷）。
83. **[代码]** `Store.set` 静默吞异常：配额满/隐私模式下进度无声丢失，无任何警示。
84. **[代码]** `Meta.load` 浅合并：导入的半截存档（缺 settings 子字段）会整段顶掉默认值。
85. **[代码]** 「焚稿重开」只清 meta 不清 run 存档（`reset` 回调只 `Meta.save(fresh())`）——孤儿续行档残留，焚稿后「续行」仍可进旧局。
86. **[代码]** `smoke.mjs` 存在隐式全局 `sawDescendFlag = true`（页面非严格态侥幸不炸）。
87. **[代码]** `tests/harness.mjs` 的 `placeEnemy` 不校验落点（可把怪塞进墙里）——本次 QA 中曾因此让探针空跑（B-19 第一版）。
88. **[代码]** `tests/bughunt_ui.mjs` 的 `ok()` 极性与 pass/fail 计数混乱（QA 脚手架自身缺陷，诚实记录；结论以正文证据为准）。
89. **[文档]** 猴测输出标签「× ≤500 动」与实际上限 2500 不符（改循环没改文案）。

## K · 体验毛刺（低危补遗）

90. **[代码]** `G._visible`/`G.explored` 随存档序列化——可由地图推导的数据入库，存档体积白涨。
91. **[代码]** `serialize` 用 `JSON.parse(JSON.stringify(G))` 全量深拷贝——省事但每动一次双倍内存峰值。
92. **[代码]** 敌人死亡后尸体永久留在 `G.enemies`（只打 dead 标记），长跑档越打越重。
93. **[代码]** `fov()` 每次行动全图重算、`draw` 每帧遍历全部可见格——大层+低端机帧率隐患（无脏标记/增量）。
94. **[代码]** 伤害浮字 900ms 定长一套动效，会心/潜袭/毒蚀无差异化表现（会心仅变金色）。
95. **[代码]** 潜袭（急击勿失）触发与普攻同一条音效，+2 的爽感没有声音反馈。
96. **[代码]** 灶间火苗动画 `sin(t/160 + x)` 用 x 作相位——同列多灶同步闪烁（视觉穿帮）。
97. **[代码]** 小地图玩家点无闪烁/描边，密集探索区里不好找自己。
98. **[代码]** 顶栏特则红签的说明只放在 `title` 悬停提示——触屏设备完全不可达。
99. **[代码]** 结算页成就/解锁行无动画无分组，信息平铺拥挤。
100. **[代码]** 音效无总音量控制（只有静音二值开关），`tone` 内部硬编码 0.5 系数。

---

## 统计与建议

| 类别 | 数量 | 建议优先级 |
|---|---|---|
| A 崩溃与错误 | 3 | **P0 立修**（1-3 一处 null 守卫 + 刷新时机可全解） |
| B 死内容 | 11 | **P0**（4-8 是玩家可拾取/可遭遇的核心机制失效，属于「承诺了没做」） |
| C 死代码 | 7 | P2（清理即可，17/18 顺带补功能） |
| D 规则漏洞 | 9 | **P0/P1**（22-24 是可滥用数值洞；25-28 是回合模型瑕疵） |
| E 判定与数值 | 10 | P1（31/33 是明确实现错误；36-38 需先定设计语义） |
| F 经济与进度 | 6 | P1（42/44 直接影响体验闭环） |
| G UI/UX | 20 | P1/P2（49/50/51 建议随下版一起修） |
| H 文档不符 | 8 | P2（半天可全清） |
| I 平衡预警 | 4 | P1（75 需实测定级） |
| J 工程卫生 | 11 | P2（79/83 是数据安全类，建议尽早） |
| K 体验毛刺 | 11 | P3 |

**修复路线建议**：第一批修 A1-A3 + B4-B9 + D22-D25（玩家一小时内必撞的全部硬伤）；第二批修 E31/E33 + F42 + G49/G50（数值与文案失信）；其余按迭代消化。

*——清册完。刀有百疵，方知百炼。*

---

## 已修复 · 第二轮（2026-09-19 全流程实玩发现，18 项）

> 方法：Playwright UI 通道完整通关一遍（策略 bot），死亡回放 + 定向验证。全流程日志还催生了战术/平衡修正：大哥城墙之梦移植失控（地宫=常驻+1伤，改纯血量22）、鲁豪×2一刀6（基础刀3→2）。

| # | 原编号 | 问题 | 修复 |
|---|---|---|---|
| 1 | #4 | 刀卡「刀扫一片」死卡 | 刀击溅射身旁另一敌（同伤、不连锁） |
| 2 | #5 | 刀卡「长杆马刀」死卡 | 面前隔一格可直刺（不移动） |
| 3 | #6 | 刀卡「卧薪尝胆」死卡 | 玩家受伤积怨（≤2层），刀击+1消耗 |
| 4 | #7 | 「锦绣夜行」全轮仅首刀 | 入层重置，真·每层首刀 |
| 5 | #8 | 崇国/顾一「免死」死代码 | kill() 敌侧 lethalKeep：留血回气，每场一次 |
| 6 | #9 | 点将 wonder 被动无效 | wonder 自带血祭（3次翻倍）置于技首 |
| 7 | #22 | 血祭 1 血零成本 | 需 ≥2 血，至少自损 1 |
| 8 | #21 | 蓄力无上限囤积 | 封顶 4 层 |
| 9 | #25-28 | 撞墙/锁梯/技无目标/掷无目标白给回合 | 无效动作不推进时间（actionValid 预校验） |
| 10 | #29 | 玩家被晕无效果 | 晕眩吞掉玩家行动并消耗晕层 |
| 11 | #30 | 录技不限史官 | 限音克思（文案对齐） |
| 12 | #40 | 起义援军压特殊格 | 落点限宫室/走廊地 |
| 13 | #41 | 杂兵/精英不掉钱 | 杂兵2-3、精英6-9 |
| 14 | #43 | 结算「王元昊、王元昊」 | 同镇守去重 |
| 15 | #50 | 日志双前缀「汝击汝以刀击」 | 删多余前缀 |
| 16 | #33 | 飞瓶/陷阱致死不置 dead | playerDown 置位（上轮已修，本轮验证） |
| 17 | #31 | 「班主任的偏爱」每层+6盾 | 单发+3（上轮已修，本轮验证） |
| 18 | — | 残血硬下行落地即死（bot 自杀逻辑暴露喘息不足） | bot 策略修正 + 下行前歇灶纪律 |

*——第二轮完。全流程已可通关：九层、67 斩、八卡、名册满员。*

---

## 已修复 · 第三轮（2026-09-19 平衡整理，6 项 + 5 项曲线调整）

| # | 类型 | 问题 | 修复 |
|---|---|---|---|
| 1 | **软锁** | 敌人把灶间当墙：1 格走廊里的惊动之敌被灶间永久卡住，清不了场→梯不开→整轮卡死（实测 2.3 万轮停滞） | 灶间对敌人可通行（走过不触发） |
| 2 | 公平性 | 控制链：已晕玩家可被立刻再晕（子琛+贾瀚元轮流晕=永久罚站） | 反控制链：已晕单位不可再被晕 |
| 3 | 数值失控 | 大哥「城墙之梦」在地宫=常驻+1伤（战棋里只在外圈城墙） | 改纯血量 22，被动留风味 |
| 4 | 数值失控 | 鲁豪「大腹如斗」×2 地宫一刀6（战棋可走位、地宫必中） | 基础刀 3→2（翻倍后 4） |
| 5 | 平衡 | 击破回血 2 低于群战消耗，前期必然磨死 | 击破回血 2→3（庆功之宴 3→5） |
| 6 | 平衡 | 史官 18 血对上陷阱+层深+dyad 三重新增压力过脆 | 基础血 18→20 |
| 7 | 曲线 | 第 2 层杂兵 4 只过密（前期最陡） | 4→3，压力后移给层深递增 |
| 8 | 平衡 | 层深无递增：滚雪球后 8/9 层反而轻松 | 敌人血量 +5%/层（第 9 层 +40%） |
| 9 | 经济 | 钱囤 360 无处花 | 商店新增「大鸡腿」（22 钱/回 16 血） |
| 10 | 可玩性 | 录技 21 个但技能栏溢出、热键只有 9 个 | 技能栏换行显示全部（U/I/O/P+1-9，其余鼠标） |
| 11 | 一致性 | 血性修炼只对史官生效，wonder 血祭吃不到 | 凡持血祭之将皆适用 |
| 12 | 一致性 | 下行喘息 25% 对齐不了新曲线 | 25%→35% |

*——第三轮完。全流程通关已被验证（九层 67 斩八卡）；bot 在第 2-3 层镇守决斗处于剃刀边缘，弹反窗口利用率是胜负手。*
